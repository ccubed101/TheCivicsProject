This directory contains the files that are required to implement a Docker container 
that clones the project respository entirely within the container.  In that sense 
the container is more self-contained and it does not need to be aware of a repository
that exists on disk.